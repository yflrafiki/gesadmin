version: '3.8'

services:
  frontend:
    build: .
    container_name: ges_hr_admin_frontend
    restart: always
    ports:
      - "8080:80"
    networks:
      - ges_network

networks:
  ges_network:
    external: true
